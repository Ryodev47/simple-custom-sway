# Backups created by install.sh
*.bak
*.bak.*

# OS / editor junk
.DS_Store
*.swp
*.swo
*~

# Local, machine-specific overrides (keep these out of version control)
.config/**/local.conf
.config/**/*.local

# Screenshots saved inside the repo via the grim/slurp keybinding
assets/screenshot-*.png
